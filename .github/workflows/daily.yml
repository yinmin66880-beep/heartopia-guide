name: daily-intel

# 每日情报自动更新管道：
#   北京时间 06:10（UTC 22:10）—— 物资点位 06:00 刷新后
#   北京时间 18:10（UTC 10:10）—— 兑换码 18:00 更新后
# 抓取成功后自动 commit data/daily.*，若仓库开启 GitHub Pages 会随之自动重新部署。
# 注意：GitHub 定时任务可能有数分钟延迟；公开仓库 60 天无活动会暂停定时任务。

on:
  schedule:
    - cron: '10 22 * * *'
    - cron: '10 10 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-intel

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run daily intel pipeline
        run: python scripts/fetch_daily.py

      - name: Commit and push if changed
        id: commit
        run: |
          git config user.name "heartopia-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/daily.js data/daily.json
          if git diff --cached --quiet; then
            echo "今日数据无变化，跳过提交"
          else
            git commit -m "chore(daily): auto update intel $(date -u +%F)"
            git push
            echo "pushed=true" >> "$GITHUB_OUTPUT"
          fi

      # 等待 Cloudflare Pages 完成自动部署（静态站通常 1-2 分钟），确保 key 文件与最新数据已上线
      - name: Wait for deploy
        if: steps.commit.outputs.pushed == 'true'
        run: sleep 180

      # IndexNow：通知 Bing/Yandex/Naver 等重新抓取今日更新的页面
      - name: Ping IndexNow
        if: steps.commit.outputs.pushed == 'true'
        run: python scripts/ping_indexnow.py
